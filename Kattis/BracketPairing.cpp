#include <bits/stdc++.h>
#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <unordered_set>
using namespace std;

// set<char> bracket = {'(', ')', '[', ']', '{', '}'};

unordered_set<char> bracket = {'(', ')', '[', ']', '{', '}'};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    string n;
    cin >> n;

    // for (int i = 0; i <= n.size(), i++)
    // {
    //     if
    //         [i] in
    // }

    return 0;
}