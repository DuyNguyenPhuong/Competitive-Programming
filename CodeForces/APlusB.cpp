#include <bits/stdc++.h>
#include <iostream>
#include <vector>
#include <string>
using namespace std;
#define cp(t) \
    int t;    \
    cin >> t; \
    while (t--)

int solve()

{
    int a, b;
    scanf("%d+%d", &a, &b);
    cout << a + b << "\n";
    // string n;
    // cin >> n;

    // cout << int(n[0]) - 48 + int(n[2]) - 48 << endl;
    return 0;
}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}