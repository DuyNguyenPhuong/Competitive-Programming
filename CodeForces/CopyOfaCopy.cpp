#include <bits/stdc++.h>
#include <iostream>
#include <vector>
#include <string>
#include "../Programming Library/grid_iterator.cpp"
#define ll long long
using namespace std;
#define cp(t)    \
    long long t; \
    cin >> t;    \
    while (t--)

int main()
{
    int n, m, k;
    cin >> n >> m >> k;
    // vector<vector<vector<string>>> v(k, vector<string>(n));

    // cin >> v;
    GridIterator gi(n, m);
}